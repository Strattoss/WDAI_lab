export interface Trip {
    name: string;
    destination: string;
    startDate: Date;
    endDate: Date;
    unitPrice: number;
    freeSeats: number;
    description: string;
    imgPath: string;
}